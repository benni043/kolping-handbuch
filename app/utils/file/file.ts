export type Item = {
  name: string;
  type: "dir" | "file";
  size: number;
};

// const a = document.createElement("a");
// a.href = `/api/files?path=${encodeURIComponent(link)}`;
// a.target = "_blank";
// a.click();
